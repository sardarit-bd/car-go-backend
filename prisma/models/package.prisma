model ProtectionPackage {
    id          String    @id @default(cuid())
    name        String
    description String[]  // PostgreSQL array for list of coverage details
    price       Decimal   @db.Decimal(10, 2)
    createdAt   DateTime  @default(now())
    updatedAt   DateTime  @updatedAt
    deletedAt   DateTime?
}